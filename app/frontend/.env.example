# =============================================================================
# Frontend environment variables (Next.js / API proxy).
# Copy to .env or .env.local and adjust. See also root .env.example for Docker.
# =============================================================================

# -----------------------------------------------------------------------------
# API proxy (app/api/[[...path]]/route.ts)
# -----------------------------------------------------------------------------
# Backend URL used to forward /api/* requests. Read at request time.
# Fallback order: BACKEND_URL → NEXT_PUBLIC_API_URL → http://localhost:8080.
BACKEND_URL=http://localhost:8080

# Optional. Public API base URL (exposed to browser). Used as fallback when
# BACKEND_URL is not set (e.g. in some build or server contexts).
NEXT_PUBLIC_API_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Suggested values by environment
# -----------------------------------------------------------------------------
# Local dev, backend on host:        BACKEND_URL=http://localhost:8080
# Frontend in Docker, backend on host: BACKEND_URL=http://10.66.50.210:8080
# Frontend and backend in Docker:     BACKEND_URL=http://backend:8080
