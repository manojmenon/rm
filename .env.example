# =============================================================================
# Combined env example for Docker Compose and local runs.
# Per-service examples: config/env/backend.env.example, config/env/frontend.env.example,
# config/env/database.env.example, config/env/observability.env.example
# =============================================================================
# Copy to .env and set values. Docker Compose reads .env from project root.

PORT=8080
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=roadmap
DB_SSLMODE=disable
JWT_SECRET=change-me-in-production
LOG_LEVEL=info
LOG_FORMAT=json
OTEL_EXPORTER_OTLP_ENDPOINT=

POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=roadmap

BACKEND_URL=http://backend:8080
NEXT_PUBLIC_API_URL=http://localhost:8080

GRAFANA_PASSWORD=admin
