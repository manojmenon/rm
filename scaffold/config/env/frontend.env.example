# =============================================================================
# Frontend (Next.js) — API proxy and public URL
# =============================================================================
# Backend URL for /api/* proxy. Fallback: NEXT_PUBLIC_API_URL → http://localhost:8080
BACKEND_URL=http://localhost:8080
NEXT_PUBLIC_API_URL=http://localhost:8080

# Local dev, backend on host:     BACKEND_URL=http://localhost:8080
# Frontend in Docker, backend on host: BACKEND_URL=http://<host-ip>:8080
# Full Docker Compose:            BACKEND_URL=http://backend:8080
