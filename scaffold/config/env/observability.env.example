# =============================================================================
# Observability (Grafana, optional overrides for Docker Compose)
# =============================================================================
# Grafana admin password
GRAFANA_PASSWORD=admin

# Optional: override backend DB when Postgres runs on host
# DB_HOST=10.66.50.210

# Optional: override when frontend in Docker, backend on host
# BACKEND_URL=http://10.66.50.210:8080
