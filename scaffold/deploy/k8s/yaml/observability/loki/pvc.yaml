apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-data
  namespace: roadmap
  labels:
    app: loki
    app.kubernetes.io/name: roadmap
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
